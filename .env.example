# MongoDB Atlas 연결 문자열 (클릭 수 집계에 사용)
# 설정하지 않으면 페이지는 정상 동작하고 클릭 수 집계만 비활성화됩니다.
MONGODB_URI="mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority"

# 사용할 데이터베이스 이름 (기본값: linknamu)
MONGODB_DB="linknamu"
